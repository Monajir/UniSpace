# Leave blank when the frontend and backend share an origin or the Vite proxy is used.
VITE_API_BASE_URL=

# Development-only target used by the Vite proxy.
VITE_DEV_PROXY_TARGET=http://localhost:8082
